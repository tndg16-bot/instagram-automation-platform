# 引き継ぎ書 - 2026年1月21日作業用

## 🔧 優先度高: バグ修正
**日報自動生成が動作していない問題**
- 詳細: [[instagram/docs/daily_sync_issue_report|問題調査レポート]]
- 主な原因: パス設定とrun_sync.batの破損
- 所要時間: 約15分

---

## 🎯 明日の目標
**Phase 4: 収益化 & サービス強化** の実装開始

---

## 📋 現在の状態

### 完了済み (着手不要)
- Phase 1: MVP基盤 ✅
- Phase 3: ワークフロー機能 ✅

### 次に着手すべきタスク (Phase 4)
1. **Frontend (Agent 1)**
   - サービス紹介ページ (`src/app/services/page.tsx`)
   - 商品一覧ページ (`src/app/products/page.tsx`)
   - チェックアウトページ (`src/app/checkout/page.tsx`)

2. **Backend (Agent 2)**
   - 決済サービス (`src/services/paymentService.ts`)
   - 注文API (`src/api/routes/orders.ts`)
   - 予約API (`src/api/routes/bookings.ts`)

3. **Sub/AI (Agent 3)**
   - セールスFAQノード (`src/services/nodes/salesNode.ts`)
   - レコメンドサービス (`src/services/recommendationService.ts`)
   - フォローアップノード (`src/services/nodes/followUpNode.ts`)

---

## 🚀 エージェント起動コマンド

### 事前準備
```powershell
cd "c:\Users\chatg\Obsidian Vault\papa\Apps\Tools\instagram"
```

### Terminal 1: Frontend (Agent 1)
```
cd "c:\Users\chatg\Obsidian Vault\papa\Apps\Tools\instagram\frontend"

Phase 4のタスクを開始します。以下を順番に実装してください：

1. サービス紹介ページ (src/app/services/page.tsx)
   - コーチング・コンサルティングサービスの詳細紹介
   - CTAボタン（問い合わせ・予約）を配置

2. 商品一覧ページ (src/app/products/page.tsx)
   - 有料コンテンツ（電子書籍、動画教材）のカード一覧
   - 価格表示、購入ボタン

3. チェックアウトページ (src/app/checkout/page.tsx)
   - カート確認 → 決済入力（Mock UI）→ 完了画面
   - POST /api/orders を呼び出す

各タスクはGitHub Issue作成 → ブランチ作成 → 実装 → PR → マージの流れで進めてください。
日本語でコミット・対話してください。
```

### Terminal 2: Backend (Agent 2)
```
cd "c:\Users\chatg\Obsidian Vault\papa\Apps\Tools\instagram\backend"

Phase 4のタスクを開始します。以下を順番に実装してください：

1. 決済サービス (src/services/paymentService.ts)
   - createPaymentIntent(amount, currency) - Mockモードでは成功を返す
   - confirmPayment(intentId) - 決済確定

2. 注文API (src/api/routes/orders.ts)
   - POST /api/orders - 注文作成
   - GET /api/orders - ユーザーの注文履歴
   - GET /api/orders/:id - 注文詳細

3. 予約API (src/api/routes/bookings.ts)
   - POST /api/bookings - 予約作成
   - GET /api/bookings/availability - 空き枠取得（Mock）
   - PUT /api/bookings/:id/cancel - 予約キャンセル

各タスクはGitHub Issue作成 → ブランチ作成 → 実装 → PR → マージの流れで進めてください。
日本語でコミット・対話してください。
```

### Terminal 3: Sub/AI (Agent 3)
```
cd "c:\Users\chatg\Obsidian Vault\papa\Apps\Tools\instagram\backend"

Phase 4のタスクを開始します。以下を順番に実装してください：

1. セールスFAQノード (src/services/nodes/salesNode.ts)
   - ユーザーの質問を分類（価格/サービス内容/予約方法/その他）
   - 質問タイプに応じた回答テンプレートを返す

2. 商品レコメンドサービス (src/services/recommendationService.ts)
   - ユーザー属性に基づいて商品を推薦
   - getRecommendations(userId) → Product[]

3. 購入後フォローアップノード (src/services/nodes/followUpNode.ts)
   - 購入完了後にDMでサンクスメッセージを送信

各タスクはGitHub Issue作成 → ブランチ作成 → 実装 → PR → マージの流れで進めてください。
日本語でコミット・対話してください。
```

---

## 📁 重要ファイルの場所
- タスク管理: `C:\Users\chatg\.gemini\...\task.md`
- 実行ログ: `walkthrough.md`
- Backend: `instagram/backend/src/`
- Frontend: `instagram/frontend/src/app/`

## ⚠️ 注意事項
- Backendサーバーは `npm run dev` ではなく `npx ts-node src/server.ts` で起動推奨
- 環境変数: `$env:MOCK_MODE='true'` を設定してから起動
